<IfModule mod_rewrite.c>
    Options -MultiViews

RewriteEngine On

# Mengabaikan file dan folder yang memang ada secara fisik
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Mengarahkan sisanya ke index.php
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]
</IfModule>